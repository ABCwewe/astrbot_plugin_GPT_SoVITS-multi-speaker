# Changelog

本文件记录 `astrbot_plugin_GPT_SoVITS-multi-speaker` 的版本变更历史。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [v3.3.0] - 2026-07-26

### 新增

- **可视化配置页面（Plugin Pages）**：新增 `pages/settings/` 前端页面，提供简约美观的 WebUI 可视化配置体验
  - 6 个标签页：基础设置、说话人管理、TTS 参数、自动触发、情感判断、缓存设置
  - 支持亮色/暗色主题自动切换
  - 响应式布局，移动端友好
- **情绪可视化编辑器**：情绪配置从 JSON 代码编辑器升级为卡片式可视化编辑器
  - 可折叠卡片展示每个情绪，头部显示名称和关键词标签
  - 关键词标签式输入：回车添加，点击 × 删除
  - 语速、片段间隔使用滑块直观调节
- **Web API 端点**：注册 3 个后端 API 供前端页面调用
  - `GET /config`：获取完整配置（emotions JSON 自动解析为数组）
  - `POST /config`：保存配置（emotions 数组自动序列化为 JSON 字符串，保存后热重载服务）
  - `GET /providers`：获取可用 LLM 提供商列表
- **i18n 国际化**：新增 `.astrbot-plugin/i18n/zh-CN.json` 和 `en-US.json`
- **CHANGELOG.md**：新增版本变更记录文件

### 变更

- **「说」指令重构为 on-message hook**：从 `@filter.command("说")` 改为 `@filter.event_message_type`，支持自然语言触发结构
  - 新格式：`<说话人><情绪>说 <文本>`，例如 `丹瑾生气说 不喜欢你`
  - 无需唤醒前缀，直接在消息中匹配
  - 说话人名称和情绪名称按长度优先匹配，避免短名称误匹配
  - 解析逻辑从空格分割改为前缀匹配，支持说话人名/情绪名与「说」字紧密相连
- **`_conf_schema.json` 调整**：除 `enabled`（总开关）外所有配置项设为 `invisible: true`，统一通过 Pages 页面管理
  - 保留完整 schema 结构以确保配置默认值和迁移逻辑不受影响
- **README.md 更新**：文档反映新版本功能、语法和配置方式

### 修复

- 无

---

## [v3.2.2] - 2025

### 新增

- **指令触发可以不调用情感识别**：增加 `judge.enabled_command` 开关控制
- **只有一个情感预设跳过 LLM**：避免 LLM 浪费

### 修复

- **修复模型加载**：现在不会只加载默认模型了

---

## [v3.2.1] - 2025

### 变更

- **GSV 列表输出到 node（合并转发）**
- **GSV 列表显示可用情绪**

### 修复

- 修复指令无输出
- 修复指令重复注册

---

## [v3.2.0] - 2025

### 变更

- 修改 GSV 命令逻辑，「GSV 列表」现在会显示可用情绪
- 修复切换模型的问题，修改默认情绪的逻辑

---

## [v3.1.0] - 2025

### 新增

- 多说话人支持
- 情绪参数切换（按关键词或 LLM 判别情绪）
- 自动转语音（概率触发）
- LLM 工具调用（`gsv_tts`）
- 语音缓存
- 配置生成脚本 `generate_speakers_config.py`
- 旧配置自动迁移
